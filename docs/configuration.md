WIP

```js
<strong>
  styled-components.browser.esm.js:1951 It looks like you've wrapped styled() around your React component (StyleGuideRendererUnstyled), 
  but the className prop is not being passed down to a child. No styles will be rendered unless className is composed within your React component.
</strong>
```
